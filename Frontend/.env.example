# Base URL for the Elsie Lash Studio backend API.
# Used by the frontend to call GET /api/services, POST /api/bookings, GET /api/gallery, etc.
# Local dev default (used if unset): http://localhost:5001
NEXT_PUBLIC_API_BASE_URL=http://localhost:5001
